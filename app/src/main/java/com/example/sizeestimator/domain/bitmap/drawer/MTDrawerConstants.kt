package com.example.sizeestimator.domain.bitmap.drawer

import android.graphics.Color

object MTDrawerConstants {
    const val BOX_STROKE_WIDTH_PX = 2F
    val MARK_UP_COLORS: List<Int> =
        listOf(
            Color.RED,
            Color.YELLOW,
            Color.BLUE,
            Color.CYAN,
            Color.BLACK,
            Color.DKGRAY,
            Color.GRAY,
            Color.GREEN,
            Color.LTGRAY,
            Color.MAGENTA
        )
}